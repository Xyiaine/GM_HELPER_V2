node_modules/
dist/
.env
server/uploads/
.DS_Store

# Agent workspace (memoire projet, non versionnee)
.workbuddy-ai/

# Fichiers temporaires de build Vite
vite.config.js.timestamp-*.mjs

# Base de donnees locale (contient des donnees utilisateur : non versionnee)
# schema.prisma est la source de verite, la base se recree avec `npx prisma db push`
server/prisma/dev.db
server/prisma/dev.db.backup-*

# Dossier de build de verification (contournement du blocage de dist)
client/.build-check/
